import { WordWrapTooltipDirective } from './word-wrap-tooltip.directive';

describe('WordWrapTooltipDirective', () => {
  // it('should create an instance', () => {
  //   const directive = new WordWrapTooltipDirective();
  //   expect(directive).toBeTruthy();
  // });
});
